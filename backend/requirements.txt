fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
biopython>=1.83
scikit-learn>=1.3
numpy>=1.26
pytest>=8.0
httpx>=0.27
psycopg2-binary>=2.9   # only needed when DATABASE_URL points at Postgres

# Real embedding model (app/pipeline/embedding.py). Only needed when
# USE_PRETRAINED_EMBEDDINGS=true — install these on a machine with internet
# access to huggingface.co, not required for local k-mer-fallback dev.
torch>=2.2
transformers>=4.40
einops>=0.7            # required by DNABERT-S's trust_remote_code model code

requests>=2.31          # only needed to run scripts/build_reference_db.py
openpyxl>=3.1            # for GET /samples/{id}/export?format=xlsx
