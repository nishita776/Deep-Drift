# Copy this to .env and adjust as needed. Uncommented defaults below match
# local (non-Docker) SQLite development — no changes needed to just run it.

# DATABASE_URL=sqlite:///./dev.db
# DATABASE_URL=postgresql://novataxa:novataxa@localhost:5432/novataxa

# STORAGE_DIR=storage
# REFERENCE_FASTA=app/data/sample_reference.fasta
# CONFIDENT_MATCH_THRESHOLD=0.85
# KMER_SIZE=4
# MIN_CLUSTER_SIZE=3
